export const calculateBonus = (a, b) => {
    let bonus;
    const sum = a + b;
    debugger; // paused on debagger statment
    sum > 50 ? (bonus = 50) : (bonus = sum);
    debugger; //paused on debagger statment
    return bonus;
    };

    



